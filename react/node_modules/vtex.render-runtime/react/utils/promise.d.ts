declare type Resolver<T> = T extends void ? (resolve: () => void) => void : (resolve: (value: T) => void) => void;
export declare const promised: <T = void>(resolver: Resolver<T>) => Promise<T>;
export {};
