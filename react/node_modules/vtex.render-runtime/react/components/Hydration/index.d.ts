import { FunctionComponent } from 'react';
import { Extension } from '../../typings/runtime';
interface Props {
    treePath: string;
    hydration: Extension['hydration'];
}
declare const Hydration: FunctionComponent<Props>;
export default Hydration;
